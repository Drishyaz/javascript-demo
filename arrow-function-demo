// using normal function
function add (a,b)
{
  return a + b;
}
console.log(add(10,50));

// using arrow function
const add = (a,b) => a + b;
console.log(add(10,5));

// using arrow function allows us to omit the function and return keyword, and finish the function definition within one line
